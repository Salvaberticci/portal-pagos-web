# Archivos Sensibles y Pesados (IGNORAR)
uploads/
paginas/config.php
paginas/conexion.php
.env
*.zip
node_modules/
*.sql
!database/migrations/*.sql

# Logs (IGNORAR para evitar conflictos en hosting)
paginas/principal/logs/
*.log

# Cache Dinámica (IGNORAR)
portal/tasa_cache.json

# Configuración sensible (API keys, etc.) — crear manualmente en cada entorno
config/wisphub_credentials.php
!config/wisphub_credentials.sample.php
